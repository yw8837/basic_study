# 문제 생성 스펙 (LLM 기반)

초보자 학습용 복습 퀴즈 문제를 **수업 자료(md)로부터 LLM이 생성**할 때 따르는 규칙.
생성=Sonnet, 검수=Haiku.

## 목적
이어드림스쿨 AI 과정 수업 정리(블로그 글)를 복습하기 위한 **초보자용 퀴즈**.
학습자가 핵심 개념·문법을 스스로 점검하도록 돕는 게 목표.

## 입력
- `C:\Users\최용우\yearstudy_6\content\posts` 아래의 검증된 수업 md.
- `미션` 경로는 제외.

## 핵심 규칙 (할루시네이션 방지) — 최우선
1. **자료에 명시된 내용으로만 출제.** 자료에 없는 사실·코드·개념·수치를 새로 지어내지 말 것.
2. **교육 범위를 벗어나지 말 것.** 단, 자료 안의 `참고자료` 섹션은 이미 검토·검증된 내용이므로 출제에 활용 가능.
3. **정답은 반드시 자료에 근거.** 코드 문제의 정답은 실제 실행 결과와 일치해야 함. 추측 금지.
4. **초보자 수준 유지.** 함정 문제·지엽적 암기·자료에서 안 다룬 심화 금지.
5. **오답(distractor)은 그럴듯하게.** 초보자가 흔히 헷갈리는 개념, 관련 있는 값/문법으로. 무관하거나 황당한 오답 금지.
6. **문제는 self-contained.** 코드 문제는 변수 정의 등 푸는 데 필요한 맥락을 모두 포함. 출력을 묻는다면 그 출력을 만드는 코드가 문제 안에 다 있어야 함.
7. **중복 금지.** 같은 개념을 유형만 바꿔 반복 출제하지 말 것. 한 개념은 가장 적합한 한 유형으로.

## 문제 유형 (5종)
| type | 설명 | grading |
|---|---|---|
| `fill_blank` | 코드 빈칸 채우기(객관식 4지선다) | `code_strict` |
| `code_to_result` | 코드 보고 출력 예측(객관식) | `code_strict` |
| `result_to_code` | 출력 보고 코드 고르기(객관식) | `code_strict` |
| `concept_mc` | 개념·용어 객관식(4지선다) | `lenient` |
| `short_answer` | 짧은 용어 단답(직접 입력) | `lenient` |

- 객관식은 **보기 4개**(정답 1 + 오답 3), 정답도 보기 안에 포함.
- `short_answer`는 **답이 짧고 명확한 용어**일 때만(한두 단어). 서술형 답이 필요하면 `concept_mc`로.

## 분량
- 초보자 핵심 위주. **과잉 생성 금지.**
- 수업 파일당 대략 **15~30문제** 목표(자료 분량에 따라 가감). 양보다 질.
- 한 섹션에서 같은 유형을 2개 넘기지 말 것(권장).

## 출력 JSON 스키마
각 문제는 아래 객체. 파일 전체는 객체들의 배열(JSON array).
```json
{
  "type": "fill_blank | code_to_result | result_to_code | concept_mc | short_answer",
  "question": "문제 본문 (코드는 ```python 코드블록``` 사용)",
  "answer": "정답 (문자열)",
  "options": ["보기1", "보기2", "보기3", "보기4"],
  "grading": "code_strict | lenient",
  "source_file": "5-29 파이썬 기초.md",
  "section": "해당 수업 섹션 제목",
  "explanation": "왜 그 답인지 초보자 눈높이 해설 (자료 근거)"
}
```
- `short_answer`는 `options`를 빈 배열 `[]`로.
- `id`는 비워둠 — 생성 후 별도 스크립트가 부여.

## 검수 단계 (Haiku, 별도 패스)
생성된 각 문제에 대해 확인:
- [ ] 정답이 자료/코드 실행 결과와 실제로 일치하는가?
- [ ] 문제가 self-contained한가(맥락 충분)?
- [ ] 오답이 그럴듯한가(무관/중복 아님)?
- [ ] 자료 범위를 벗어난 내용이 없는가?
- [ ] 초보자 수준에 맞는가?

문제가 있으면 수정안 또는 제거 사유를 남길 것.
